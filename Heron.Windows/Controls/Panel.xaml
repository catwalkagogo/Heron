<UserControl x:Class="CatWalk.Heron.Windows.Controls.Panel"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:CatWalk.Heron.Windows.Controls"
			 xmlns:win="clr-namespace:CatWalk.Heron.Windows"
			 win:PluginExtensions.NotifyCreatedEvent="True"
			 mc:Ignorable="d" 
			 x:Name="_this"
			 d:DesignHeight="300" d:DesignWidth="300">

	<UserControl.InputBindings>
		<KeyBinding Key="Tab" Command="{Binding FocusNextPanelCommand, ElementName=_this}"></KeyBinding>
	</UserControl.InputBindings>
	
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"></RowDefinition>
			<RowDefinition Height="*"></RowDefinition>
			<RowDefinition Height="auto"></RowDefinition>
		</Grid.RowDefinitions>

		<local:AddressBar Grid.Row="0"></local:AddressBar>

		<local:EntryListView
		x:Name="_ListView"
		Grid.Row="1"
		DataContext="{Binding ListView}">
		</local:EntryListView>
	</Grid>

</UserControl>
