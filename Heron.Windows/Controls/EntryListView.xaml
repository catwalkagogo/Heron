<UserControl x:Class="CatWalk.Heron.Windows.Controls.EntryListView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 mc:Ignorable="d" 
			 d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>
		<ResourceDictionary>
			<CollectionViewSource x:Key="ChildrenView" x:Name="_ChildrenView" Source="{Binding CurrentEntry.Children}">

			</CollectionViewSource>
		</ResourceDictionary>
	</UserControl.Resources>

	<ListView
		x:Name="_ListView"
		HorizontalAlignment="Stretch"
		VerticalAlignment="Stretch"
		ItemsSource="{Binding Source={StaticResource ChildrenView}}">
		
	</ListView>
</UserControl>
